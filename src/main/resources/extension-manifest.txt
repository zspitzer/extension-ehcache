Manifest-Version: 1.0
Built-Date: ${build.timestamp}
version: "${project.version}"
id: "${extensionId}"
name: "${label}"
description: "${description}"
start-bundles: false
release-type: ${releaseType}
lucee-core-version: "${luceeCoreVersion}"
cache: "[{'class':'org.lucee.extension.cache.eh.EHCache','maven':'${project.groupId}:${project.artifactId}:${project.version}','label':'${label}','description':'${description}'}]"
